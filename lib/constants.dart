import 'package:flutter/material.dart';

const kRightAnswerIcon = Icon(Icons.check, color: Colors.green);
const kWrongAnswerIcon = Icon(Icons.close, color: Colors.red);

const kChoiceButtonColor = Color(0xFF1976D2); //Colors.blue[700];
const kChoiceButtonTextColor = Colors.white;
const kChoiceButtonTextStyle = TextStyle(fontSize: 45.0, color: Colors.white);

const kIncorrectAnswerTextStyle =
    TextStyle(color: Colors.pink, fontWeight: FontWeight.bold, fontSize: 30);

const kDialogButtonTextStyle = TextStyle(color: Colors.white, fontSize: 20);

const kQuizPageTransparentBackgroundColor =
    Color(0x3264B5F6); //Colors.blue.shade300.withAlpha(50)

const kQuizPageQuestionTextStyle =
    TextStyle(fontSize: 55.0, color: Colors.white);

const kDrawerBackgroundColor = Color(0xFF212121); //Colors.grey.shade900;
const kDrawerDividerColor = Colors.grey;
const kDrawerTextStyle = TextStyle(color: Colors.white, fontSize: 20);
const kDrawerIconColor = Colors.white;
const kDrawerIconSize = 30.0;

const kBottomContainerHeight = 30.0;
